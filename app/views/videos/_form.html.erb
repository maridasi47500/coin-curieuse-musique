<%= form_with(model: video) do |form| %>
  <% if video.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(video.errors.count, "error") %> prohibited this video from being saved:</h2>

      <ul>
        <% video.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :link, style: "display: block" %>
    <%= form.text_field :link %>
  </div>

  <div>
    <%= form.label :tone_id, style: "display: block" %>
    <%= form.select :tone_id, options_for_select(Tone.all.collect{|g|[g.name,g.id]}), {include_blank: true} %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
