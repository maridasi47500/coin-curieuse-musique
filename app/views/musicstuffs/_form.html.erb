<%= form_with(model: musicstuff) do |form| %>
  <% if musicstuff.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(musicstuff.errors.count, "error") %> prohibited this musicstuff from being saved:</h2>

      <ul>
        <% musicstuff.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>



  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>
  <div>
    <%= form.label :image, style: "display: block" %>
    <%= form.file_field :image %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
