<%= form_with(model: instrument) do |form| %>
  <% if instrument.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(instrument.errors.count, "error") %> prohibited this instrument from being saved:</h2>

      <ul>
        <% instrument.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :familier, style: "display: block" %>
    <%= form.text_field :familier %>
  </div>
  <div>
    <%= form.label :tone_id, style: "display: block" %>
    <%= form.select :tone_id, options_for_select(Tone.all.collect{|g|[g.name,g.id]}), {include_blank: true} %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
